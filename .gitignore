# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Environment
.env
.env.local

# Penny secrets — never commit these
secrets.env

# Google OAuth token — contains live credentials
google_token.json
**/google_token.json

# OS
.DS_Store

# Local tool state
.janitor/

# Logs
*.log

# Session files (encrypted)
docs/sessions/

# Deployed plists (contain real secrets — templates only in repo)
launchd/*.plist
